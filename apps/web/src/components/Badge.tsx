/**
 * Web Badge Component
 * Implements T040
 */
import React from 'react';
import { Badge, BadgeProps } from '@flaresmith/ui/Badge';

export const WebBadge: React.FC<BadgeProps> = (props) => <Badge {...props} />;
export default WebBadge;
