import type { EndpointDef } from "../parser";
import type { GeneratedFile } from "./zodSchemaGenerator";

/**
 * T086: MCP tool descriptor generator (stub)
 */
export async function generateMcpToolDescriptors(_endpoints: EndpointDef[]): Promise<GeneratedFile[]> {
  return [];
}
