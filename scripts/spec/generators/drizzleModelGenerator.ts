import type { EndpointDef } from "../parser";
import type { GeneratedFile } from "./zodSchemaGenerator";

/**
 * T083: Drizzle model code generator (stub)
 */
export async function generateDrizzleModels(_endpoints: EndpointDef[]): Promise<GeneratedFile[]> {
  return [];
}
