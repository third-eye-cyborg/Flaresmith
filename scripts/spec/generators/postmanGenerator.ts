import type { EndpointDef } from "../parser";
import type { GeneratedFile } from "./zodSchemaGenerator";

/**
 * T085: Postman collection generator (stub)
 */
export async function generatePostmanCollections(_endpoints: EndpointDef[]): Promise<GeneratedFile[]> {
  return [];
}
