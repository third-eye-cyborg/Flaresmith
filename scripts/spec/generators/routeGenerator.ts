import type { EndpointDef } from "../parser";
import type { GeneratedFile } from "./zodSchemaGenerator";

/**
 * T084: Hono route stub generator (stub)
 */
export async function generateRoutes(_endpoints: EndpointDef[]): Promise<GeneratedFile[]> {
  return [];
}
