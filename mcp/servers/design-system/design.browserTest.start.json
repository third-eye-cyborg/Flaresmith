{
  "name": "design.browserTest.start",
  "description": "Starts a browser MCP test session for a given story.",
  "inputSchema": {
    "$ref": "packages/types/src/design-sync/browserSession.ts#BrowserSessionStartRequest"
  }
}
{
  "name": "design.browserTest.start",
  "description": "Starts a browser MCP test session for a given story.",
  "inputSchema": {
    "type": "object",
    "required": ["storyId"],
    "properties": {
      "storyId": { "type": "string" },
      "authProfile": { "type": "string" }
    }
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "sessionId": { "type": "string" },
      "status": { "type": "string" },
      "startedAt": { "type": "string" }
    }
  },
  "specRef": "specs/006-design-sync-integration/spec.md"
}
