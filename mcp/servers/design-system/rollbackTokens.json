{
  "name": "design.rollbackTokens",
  "description": "Rollback design tokens to a previous version. Creates new immutable snapshot. Only platformOwner or designDelegate can rollback prod environment. Target SC-010: completes in ≤60s.",
  "inputSchema": {
    "$ref": "file://../../packages/types/src/designSystem.ts#RollbackRequest"
  },
  "outputSchema": {
    "$ref": "file://../../packages/types/src/designSystem.ts#RollbackResponse"
  },
  "examples": [
    {
      "name": "Rollback to version 10",
      "input": {
        "targetVersion": 10,
        "rationale": "Revert experimental glass tokens causing performance issues"
      },
      "output": {
        "previousVersion": 12,
        "newVersion": 13,
        "hash": "a1b2c3d4e5f6...",
        "durationMs": 1850
      }
    }
  ]
}
