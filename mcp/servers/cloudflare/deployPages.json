{
  "name": "cloudflare.deployPages",
  "description": "Deploys (idempotent) a Cloudflare Pages project (creates if missing) and returns deployment metadata",
  "inputSchema": { "$ref": "packages/types/src/mcp/cloudflare.ts#DeployPagesInput" },
  "outputSchema": { "$ref": "packages/types/src/mcp/cloudflare.ts#DeployPagesOutput" }
}
