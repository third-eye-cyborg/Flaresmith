{
  "name": "cloudflare.deployWorker",
  "description": "Deploys (idempotent) a Cloudflare Worker script and returns deployment metadata",
  "inputSchema": { "$ref": "packages/types/src/mcp/cloudflare.ts#DeployWorkerInput" },
  "outputSchema": { "$ref": "packages/types/src/mcp/cloudflare.ts#DeployWorkerOutput" }
}
